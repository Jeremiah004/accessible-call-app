// Fill these in once your backend is deployed (e.g. to Render).
// APP_SHARED_SECRET must match the same value in the backend's .env file.
window.APP_CONFIG = {
  BACKEND_HTTP: "https://YOUR-BACKEND-HOST.onrender.com",
  BACKEND_WS: "wss://YOUR-BACKEND-HOST.onrender.com",
  APP_SHARED_SECRET: "change_this_to_something_long_and_random",
};
